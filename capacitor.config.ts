import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'achievements-of-polina',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
